/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SimpleBookingsComponent } from './simple-bookings.component';

describe('Component: SimpleBookings', () => {
  it('should create an instance', () => {
    let component = new SimpleBookingsComponent();
    expect(component).toBeTruthy();
  });
});
