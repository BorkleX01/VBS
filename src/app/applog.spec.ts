/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Applog} from './applog';

describe('Applog', () => {
  it('should create an instance', () => {
    expect(new Applog()).toBeTruthy();
  });
});
