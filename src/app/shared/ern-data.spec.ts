/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {ERNData} from './ern-data';

describe('ERNData', () => {
  it('should create an instance', () => {
    expect(new ERNData()).toBeTruthy();
  });
});
